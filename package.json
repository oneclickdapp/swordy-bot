{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "scripts": {
    "heroku-prebuild": "cd bot && yarn install && exit 0;"
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.26.2"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  }
}
